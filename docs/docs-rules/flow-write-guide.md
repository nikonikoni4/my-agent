# Flow 文档编写规则

**触发场景**：编写或修改 `docs/flows/` 目录下的数据流文档时

**目标**：让 AI Agent 快速定位代码位置并理解业务逻辑，将探索时间从 1-2 小时降至 15-20 分钟

---

## 一、职责范围

### 写什么

1. **Flow 对象的数据结构**（字段定义 + 关键字段说明，使用通用格式不含语言特定语法）
2. **函数签名 + 行号**（工具查询入口）
3. **高层语义**（函数作用、设计意图）
4. **数据流路径**（数据如何流转、状态如何变化）
5. **与其他数据流的耦合关系**
6. **反常设计说明**（代码实现与设计意图不一致的地方，不含正确的防御性设计）
7. **相关文档链接**（Spec、架构文档、ADR）

### 不写什么

1. **详细实现逻辑**（代码本身已经说明）
2. **业务规则定义**（在 Spec 中，Flow 只链接）
3. **完整调用链**（写逻辑步骤，不写函数调用链）
4. **编码规则**（在 `docs/coding-rules/` 中）

---

## 二、5 类必须记录的节点

**核心原则**：只记录影响 Flow 对象的关键节点

1. **状态明确变化**：核心状态字段变化，不含辅助字段
2. **跨模块接口**：跨第一层级文件夹（`frontend`、`api`、`mcp`、`core`）
3. **数据持久化**：写入数据库或文件系统，不含日志、缓存
4. **分支节点**：Flow 对象的业务分支，不是配置分支
5. **重要的业务集合点**：多个数据源汇聚 + 业务规则判断 + 影响后续流程

---

## 三、文档结构模板

```markdown
---
version:
created_at:
updated_at:
last_updated:
abstract:
---

## 版本

| 版本 | 更新内容 |
| ---- | -------- |
| 1.0 | 初始版本 |

# 数据流：[Flow 对象名称]

**Flow 对象**：[对象名称]
**对应 Spec**：[链接]

## [Flow 对象名称] 数据结构

\`\`\`
数据结构定义（按逻辑分组，所有字段都要列出，不含语言特定语法）

# 基本信息
field1: type  # 说明
field2: type  # 说明

# 状态跟踪
status: type  # 说明
\`\`\`

**关键字段说明**：
- `field1`：为什么重要、影响什么

## 与其他数据流的耦合

### [FlowObject] ↔ [OtherObject]

**[OtherObject] 状态字段**：状态值说明

**耦合关系**：

| [FlowObject] 状态变化 | [OtherObject] 影响 | 触发位置 |
|---------------------|------------------|---------|
| 状态A → 状态B | 影响说明 | 函数签名 |

**说明**：耦合关系的详细解释、层级关系

<key_function>
- 文件路径
  - 函数签名:行号
</key_function>

## 流程概览
[Mermaid 状态图 + note 标注]

## 数据流节点
**业务场景说明**：[列出主要链路]

## 链路 1：[场景名称]
[节点描述]

## 异常与清理
[异常处理流程]

## 反常设计说明

### [字段名或设计点]

**设计意图**：原本的设计目标
**当前实现**：实际的实现方式
**为什么是反常的**：为什么与设计意图不符
**影响范围**：对功能和理解的影响
**相关位置**：代码位置

## 相关文档

### Spec 文档
- **[标题]**：路径 - 简要说明

### 架构文档
- **[标题]**：路径 - 简要说明

### ADR
- **[标题]**：路径 - 简要说明
```

---

## 四、关键格式

### 1. key_function 标签（重要：自动更新依赖此格式）

**作用**：提供函数签名 + 行号，作为工具查询入口

**格式**（严格遵守，自动同步工具依赖此格式）：
```markdown
<key_function>
- frontend/src/layouts/ChatArea/ChatArea.tsx
  - ChatArea.handleSend:377
- agents_hub/api/services/group_chat_service.py
  - group_chat_service.GroupChatService.send_message:479
  - group_chat_service.module_level_function:202
</key_function>
```

**格式规则**（必须严格遵守）：
- 标签格式：`<key_function>` 和 `</key_function>`
- 文件路径：从仓库根目录开始，使用 `-` 开头
- 函数列表：使用 `  -` 开头（2个空格 + `-`）
- 函数签名格式（重要）：[file_name].[class_name(如果有)].[func_name]
  - 类方法：`FileName.ClassName.method_name`
  - 模块函数：`FileName.function_name`
  - **错误示例**：`stop_member`（缺少文件名）
  - **正确示例**：`group_chat.stop_member`（模块函数）、`group_chat.GroupChat.stop_member`（类方法）
- 行号：`:行号`（编辑器可点击）

**自动同步机制**：
- 读取 `docs/flows/*.md` 时 hook 自动调用 `sync_docs.py`
- 脚本解析此标签格式，从 `ast_scan_result.json` 查找函数行号
- 自动更新行号（不更新时间戳，避免频繁无意义提交）
- **警告**：格式错误会导致自动同步失败

**函数一致性原则**（重要，减少维护成本）：
- 链路描述中明确调用的函数（如 `ClassName.method()`）必须在 key_function 中声明
- 这样可以通过自动工具监控函数行号变化，判断 flow 是否需要更新
- 简单的逻辑操作用文字描述替代（如"构建 prompt"、"格式化消息"），不引用具体函数
- **例外情况**：外部库函数（如 `bare_claude_call`）、前端函数、模糊描述不需要在 key_function 中

### 2. 节点描述格式

```
编号. 函数签名()
   一句话描述作用
   状态: X→Y | 持久化: ✅/❌ | 跨模块: 模块1→模块2 或 ❌
   步骤: 逻辑步骤1 → 逻辑步骤2 → 逻辑步骤3
```

**要点**：
- 函数签名：不含文件路径（key_function 中已有）
- 步骤：写逻辑，不写调用链

---

## 五、注意事项

1. **按业务场景组织**，不按前后端分离
2. **步骤写逻辑**，不写调用链
3. **透传函数不记录**，用箭头跳过
4. **分支节点明确标注**（列出分支路径）
5. **数据结构必须完整**（所有字段 + 关键字段说明，规则文档中不含语言特定语法，但具体 flow 文档中可以使用实际的代码语法）
6. **耦合关系必须说明**（表格 + 说明）
7. **反常设计必须记录**（只记录真正的反常：字段名与行为不符、Spec 与实现有差异、未完成的设计。不记录：正确的防御性设计、合理的边界检查。不使用"可能"、"不明显"等模糊表述，必须基于代码事实）
8. **只记录关键函数**（重要原则，减少维护成本）：
   - **链路中明确调用的函数必须在 key_function 中声明**
   - 简单的逻辑操作可以用文字描述（如"构建 prompt"、"格式化消息"、"校验参数"）
   - 只记录对 Flow 对象有直接影响的函数（状态变化、持久化、跨模块、分支、集合点）
   - 工具函数、辅助函数、格式化函数通常不需要记录

---

## 六、编写 Checklist

### 开始编写前
- [ ] 确定 Flow 对象、识别业务场景
- [ ] 找到对应的 Spec 文档、相关的 ADR 文档

### 数据结构
- [ ] 列出所有字段（不能省略）、按逻辑分组
- [ ] 具体 flow 文档中可以使用实际的代码语法（如 Python 的 `@dataclass`）
- [ ] 关键字段有详细说明（为什么重要、影响什么）

### 耦合关系
- [ ] 识别所有相关的 Flow 对象
- [ ] 表格列出所有状态变化的影响、说明解释层级关系

### 收集节点时
- [ ] 只记录 5 类节点（状态变化、跨模块、持久化、分支、集合点）
- [ ] 不记录透传函数、配置分支、错误处理

### 编写节点时
- [ ] 函数签名格式正确、一句话描述作用
- [ ] 标注状态/持久化/跨模块、步骤写逻辑不写调用链

### 反常设计
- [ ] 识别设计意图与实际实现不符的地方
- [ ] 说明为什么是反常的、影响范围、指向代码位置
- [ ] 不记录正确的防御性设计、合理的边界检查
- [ ] 不使用"可能"、"不明显"等模糊表述，必须基于代码事实给出明确结论

### 完成后
- [ ] key_function 包含所有关键函数（链路中明确调用的函数）
- [ ] Mermaid 图只画核心状态转换
- [ ] 按业务场景分链路、分支节点明确标注
- [ ] 反常设计已记录、链接到相关文档
- [ ] 链路描述使用文字描述简单逻辑，减少函数引用

---

## 七、参考示例

完整示例：`docs/flows/agent-call-lifecycle.md`

---
version: 2.0
created_at: 2026-09-15
updated_at: 2026-09-17
last_updated: 撤销 v3 中"重试收尾（无人认领 / 重试耗尽）也落 llm/retry"这一越界条款——它不属于本决策的问题域，且会让 llm_retry_count 被非重试的失败虚增
abstract: session 的错误信息确定为「错误类别 + 异常链文本」的终态表达，异常链最多展开 3 层；llm/retry 只承载"第几次重试 + 触发它的错误"，非重试的失败（无人认领 / 重试耗尽）不写入该通道，其错误由 step/end 与 turn/end 的 reason_text 承载
status: decided
---

# session 错误信息记录策略

## 版本

| 版本 | 更新内容 |
| ---- | -------- |
| 1.0 | 创建文档初稿 |
| 1.1 | 重写「决策前提」：改为当前成立的事实与约束（原版把决策内容误写成前提）；决策逻辑表同步改为"前提 → 决策 → 失效切换" |
| 1.2 | 后续影响中"错误类别枚举待定义"一条补指向（已由 2026-09-15 异常分类树 ADR 明确） |
| 2.0 | 撤销「`llm/retry` 覆盖决定不重试 / 重试耗尽两个收尾出口」：该条是 v3 中由实现方在 loop 重构时自行写入的越界条款（不在本 ADR 的问题域内，也未经决策），且与 `llm_retry_count` 按记录条数统计重试次数的口径冲突。本版恢复"只在真正重试时写"，保留 `error_type` + `error_message` 两个字段 |

## 问题界定

### 问题简述

session 已承担过程日志职责（见 2026-09-03 ADR），但错误路径只把 `str(异常)` 写进 `step/end` / `turn/end` 的 `reason_text`。实测中同一条错误落进 session 的形态并不稳定：可能是底层库的裸文本（`peer closed connection without sending complete message body`），也可能是被上层包装后的摘要。结果是**只看 session 既看不出错误属于哪一类，也无法回溯根因**——与"session 即日志、只读 session 即可定位错误"的目标不符。

### 讨论范围

- 错误进入 session 的形态：记哪些字段。
- 异常链（叠层包装）在 `reason_text` 中如何呈现。
- 每次重试的原始错误（`error_type` / `error_message`）落在哪里。

### 非讨论范围

- 错误在 loop 内如何被捕获、重试、终止（含 `turn/end = error` 的语义判定）——另行决策。
- LLM 重试何时发生、算第几次（即 `llm/retry` 的**写入时机**）——属 loop 错误处理决策；本 ADR 只在这条既有语义内补字段。
- 错误类别的具体枚举清单与命名。
- 各错误类别对应的恢复策略。
- `request/error` 事件的订阅方与决策链。

### 模糊信息的明确定义

- **终态错误**：使当前 turn 结束的错误。本 ADR 只讨论其**记录形态**，不讨论判定规则。
- **发生点记录**：在事件实际发生的那一层落下的结构化记录（此处特指 `llm/retry`：**一条对应一次真正的重试**）。
- **重试次数**：本 turn 实际发生的重试次数，由 `Session.llm_retry_count(turn)` 按 `llm/retry` 记录条数统计，也是 loop 计算 `attempt` 的来源。

### 问题深度

属可观测性原则在存储层的落地：决定"日志能不能用来诊断"，影响 session 记录格式及其后续消费方（评估、统计、人工排查），是长期维护方式层面的决策。

## 现状

- session 已承担过程日志职责（2026-09-03 ADR）。
- 错误路径的终态只有 `reason_text` 一个字符串字段，没有类别字段。
- LLM 重试已有独立记录 `llm/retry`（`retry_count` + `reason`），但**不含触发重试的错误本身**，只记"第几次重试、什么决策"。
- 工具失败本身以 `ToolResult` 落在 `tool/result`（`is_error=True`），不依赖异常链承载。

## 决策前提

前提是**当前成立的事实与约束**（非决策内容）；它们成立才推出本决策，失效则应切换方案（见"决策逻辑"）。

- 前提 1（既有约束，2026-09-03 ADR）：session 是唯一的过程日志，诊断错误只能依赖 session。因此"只读 session 能定位错误"是硬需求。
- 前提 2（现状事实）：错误路径的终态目前只有 `reason_text` 一个字符串字段，没有承载错误类别的字段。
- 前提 3（实测事实）：当前错误链最长 3 层——`httpx.*` → `LLMConnectionError` → `LLmError`；`openai.APIError` → `LLMCallError` → `LLmError`。
- 前提 4（现状事实）：本仓库的错误包装一律为显式 `raise ... from e`，未使用隐式 `__context__` 链。
- 前提 5（实测事实）：`ExceptionGroup` 是容器而非链——TaskGroup 产出的 group，其 `__cause__` 与 `__context__` 均为 `None`（`suppress_context=True`），只沿 `__cause__` 遍历只能得到 `unhandled errors in a TaskGroup`。
- 前提 6（现状事实）：当前能击穿到 turn 终态的错误共 6 类（超过最大步数 / LLM 重试耗尽 / 取消 / 超时 / 工具熔断 / 工具组其他错误），其中**只有 LLM 重试**的终态是"多次失败汇总"，其余 5 类的终态就是错误本身、没有下层根因。
- 前提 7（现状事实）：LLM 重试已有独立记录通道 `llm/retry`（`retry_count` + `reason`），但其中不含触发重试的错误本身。
- 前提 8（现状事实）：工具失败以 `ToolResult` 落在 `tool/result`（`is_error=True`），其原始信息不依赖异常链承载。
- 前提 9（**假设**，待观察）：每次重试的错误可能不同（类型与内容都可能变化）。
- 前提 10（需求约束）：`error_type` 的消费方目前是人（快速定位）与评估脚本（粗粒度过滤），尚无"按根因聚合统计"的需求。
- 前提 11（现状事实，2026-09-17 补）：本 turn 的重试次数由 `Session.llm_retry_count(turn)` **按 `llm/retry` 记录条数**统计，loop 的重试序号 `attempt` 也取自它——故该通道里一旦出现非重试记录，重试次数与 `attempt` 都会虚增。

## 可选方案

### 方案 A：终态记「类别 + 异常链文本」，重试记录补上错误本身（选定）

- 终态 `turn/end` 增加 `error_type`（类别）；`reason_text` 承载异常链文本（最多 3 层）。
- `llm/retry` 增加 `error_type` + `error_message`，承载每次重试的错误本身；**写入时机不变**，仍只在决定重试时写一条。

**优势**

- 每次重试同构：一条记录对应一次重试，条数即重试次数，`llm_retry_count` 的计数口径保持精确。
- 重试通道语义单一：消费方（评估、统计）无需先判 `reason` 才知道"这是不是真的重试"。

**劣势**

- 决定终止的那次失败（无人认领 / 重试耗尽）没有结构化记录，只有 `turn/end` 的链文本——N+1 次失败中最后一次与其它次不同构。

### 方案 B：把 `llm/retry` 扩成"失败处置记录"（否决）

`llm/retry` 覆盖决定重试 / 决定不重试 / 重试耗尽三种出口，以 `reason` 区分。

**优势**：N 次重试 + 1 次终止共 N+1 条记录，每次失败同构。

**劣势**：与前提 11 直接冲突——非重试的收尾记录会让 `llm_retry_count` 虚增，而它同时是 loop 的 `attempt` 来源与统计口径；`llm/retry` 语义被摊薄；收尾错误的载体本就是 `turn/end` 的链文本，属重复。

> 本条即 v3 被选中的方案，2026-09-17 撤销（v4），理由见"演进历史"与"决策原因 4"。

### 方案 C：把异常链结构化嵌套进终态记录（否决）

终态记录直接承载 `cause` 的嵌套结构。

**劣势**：与前提 6、10 冲突——没有需要嵌套的根因层级，也没有按根因聚合的需求；schema 变成递归结构，消费方（评估脚本、统计）成本上升；稳定性差。

### 方案 D：只记 str（现状，否决）

**劣势**：违反前提 1，正是本决策要解决的问题。

## 决策逻辑

| 前提条件 | 对应决策 | 前提失效时的切换 |
|----------|----------|------------------|
| 前提 1 + 2：session 是唯一日志，且终态只有字符串字段 | 必须把"类别 + 根因"写进 session | — |
| 前提 3：错误链最长 3 层 | 链**限深 3 层** | 出现更长链 → 截断处显式标记（如 `…(还有 N 层)`）；注意截断从外往内，丢的是最内层 |
| 前提 4：错误包装只用显式 `raise ... from e` | 只沿 `__cause__` 遍历，不接 `__context__` | 出现隐式包装 → 需兼容 `__context__` |
| 前提 5：`ExceptionGroup` 不在链上 | 链遍历须对 group 特判并展开 `.exceptions` | 出现其它"容器型"错误载体 → 需扩展对应展开逻辑 |
| 前提 6 + 7 + 8：只有 LLM 重试是多层，且它已有独立通道、工具失败另走 `tool/result` | 终态只记"类别 + 异常链"，每次重试的错误交给 `llm/retry`，**不做嵌套追踪** | 出现新的多层错误 → 重新评估终态是否需跟踪下层 |
| 前提 9：每次重试的错误可能不同 | `llm/retry` 每条都带 `error_type` + `error_message` | 错误实际恒定且不在意每次重试的错误 → 可只留 `reason` |
| 前提 10：无"按根因聚合统计"的需求 | 链用**文本**而非结构化嵌套（否决方案 C） | 出现按 cause 聚合的需求 → 重新评估方案 C 的 schema 成本 |
| 前提 11：重试次数按 `llm/retry` 条数统计 | 该通道**只写重试**（否决方案 B） | 计数改为独立计数器（不再依赖记录条数）→ 可重新评估是否把收尾写进该通道 |

**防御性说明**：本 ADR 只管"记什么字段"，不管"何时写记录"。写入时机属 loop 错误处理决策，任何想扩大 `llm/retry` 写入范围的改动都必须回到那条决策线上评估，不能以"记录形态更同构"为由在此处扩。

## 演进历史

| 版本 | 方案 | 解决的问题 | 引入的新问题 |
| ---- | ---- | ---------- | ------------ |
| v1 | 终态只记 `str(异常)` | 最省事 | 只看 session 无法判断类别、无法回溯根因 |
| v2 | 终态记「类别 + 异常链」；`llm/retry` 仅记"决定重试" | 终态可读、重试次数可见 | 最后一次失败只有文本形态，与其它次不同构；其 details 丢失 |
| v3 | 终态同上；`llm/retry` 扩为"失败处置记录"（含两种收尾）——**越界写入，已由 v4 撤销** | 名义上让每次失败同构 | 重试次数被非重试记录虚增（前提 11）；通道语义被摊薄 |
| v4 | 撤销越界条款：`llm/retry` 恢复"只在真正重试时写"，保留 `error_type` + `error_message`（最终） | 重试次数与记录条数重新一致；通道语义单一 | 终止那次的失败只以 `turn/end` 的链文本存在（与 v2 同，已接受） |

**v3 的产生方式（记录归属，避免后续 agent 误当契约）**：v3 的两条越界写法是在 2026-09-15 的 loop 错误处理重构（提交 `74ab0f8`）中，由实现方在改代码的同时自行写进本文档与《agent-loop 错误处理重构》ADR 的，不是一轮独立的"用户就 `llm/retry` 语义拍板"的产物。用户未做过这个决策。

## 最终决策

当前成立的前提：前提 1~11。

因此选择 `方案 A`：

- `turn/end` 携带 `error_type`（类别）；`reason_text` 承载异常链文本，最多 3 层，超出则显式标记截断。
- 异常链遍历规则：沿 `__cause__` 逐层输出 `类型: 消息`；遇到 `BaseExceptionGroup` 则展开其 `.exceptions`（group 不在链上）。本仓库的错误包装均为显式 `raise ... from e`，故只走 `__cause__` 即可，无需接 `__context__`。
- `llm/retry` 增加 `error_type` + `error_message`；**只在决定重试时落一条**，其条数即本 turn 的重试次数（`llm_retry_count`）。无人认领与重试耗尽都不重试，故不写该记录——它们的错误由 `step/end` 与 `turn/end` 的 `reason_text` 承载。
- `step/end` 不新增 `error_type`。

前提失效时的切换路径：见决策逻辑表。

## 决策原因

- 原因 1：前提 1 + 2 下，判据是"只读 session 就能定位错误"。只记 str 时同一条错误可能呈现为底层库裸文本或被包装的摘要，类别与根因双双丢失（现状）。
- 原因 2：前提 6 + 7 + 8 下，唯一多层错误只有 LLM 重试，而它有独立的 `llm/retry` 可承载每次重试的原始信息。终态只需"类别 + 异常链"，故不做嵌套追踪。
- 原因 3：前提 10 下，异常链用文本而非结构化嵌套（对比方案 C）——文本满足"人快速定位"，结构化会引入递归 schema 与消费成本，而"按根因聚合统计"当前不是需求。
- 原因 4：前提 11 下，A 与 B 的实质差别是"重试次数还准不准"。把收尾写进 `llm/retry` 换来的是"每次失败同构"，代价却是 loop 的 `attempt` 来源与统计口径同时失真——用计数正确性换同构性不成立，故否决 B。
- 原因 5：前提 7 下，重试次数与每次重试的错误原文都应可结构化查询——这正是 `llm/retry` 存在的意义。收尾错误在该通道里是语义外的内容，而它在 `turn/end` 的链文本里已有落点。

## 后续影响

- `LLMRetryData` 需增加 `error_type` / `error_message` 字段；`TurnEndData` 需增加 `error_type` 字段。
- 需要提供一个异常链格式化函数（沿 `__cause__` 遍历、限深 3、展开 `ExceptionGroup`、处理长度与敏感信息）。
- `llm/retry` 的语义**保持**"一次重试的记录"：后续读取方（评估、统计）可以把它直接当重试次数用，无需过滤 `reason`。
- 已接受的代价：决定终止的那次失败（无人认领 / 重试耗尽）没有结构化落点，只有 `turn/end` 的 `error_type` + `reason_text` 链文本。原问题（终态看不出类别、回溯不了根因）已由 `turn/end` 覆盖。
- 错误类别的枚举清单与命名尚未定义，落地前需补齐（本 ADR 只定形态）。**部分已明确**：见 2026-09-15《异常分类树新增策略域与未知域》——新增 `AgentPolicyError`（步数上限、重试上限）与 `AgentUnknownError`（归因失败兜底）。
- 待验证：3 层上限在新增包装层后是否仍够用；截断标记能否被消费方正确识别。

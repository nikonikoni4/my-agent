# 说明
该组件用于对模型整个过程进行观察（保存），评估。
在agent loop创建之前对相关loop循环进行订阅来获取循环内部的信息

# 评估事项
## 工具调用的准确性
登记"tool/result"订阅信息，记录每个工具调用以及工具结果是否准确

准确分为2方面，但是在这里只考虑前者
1. 参数等是否正确
2. 是否应该调用这个

## 模型工具调用链路
登记loop内的事件，或者直接读取其session
获取其工具调用：1. 是否调用了无关的工具 2. 工具调用是否走的是最小路径


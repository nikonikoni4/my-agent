# 人在回路插件模块
Human-on-the-loop
在这个模块实现所有的人在回路的hooks，然后订阅回路中的waterfall事件实现人在回路
## 超过最大步数请求
当超过最大步数请求的时候，会触发MaxStepsExceededError，可以控制回路是否继续
1. 继续，并清除step累计量（如果是在本插件实现则是增加上线在下一轮turn重置上限）
2. 总结输出并停止工具调用进行汇报：这个需要取消工具schemas，让agent直接输出内容
3. 直接退出
## 工具熔断（必须开启熔断才能使用，默认不开启）
到工具连续错误时进行熔断，这个同step一样目前在tool内实现但是现在也不迁移出来
1. 继续尝试：清空熔断累计
2. 

# 已知限制
1. 本来step等累计计算都应该在本插件中实现，但是目前考虑到还不是很复杂，step控制暂时保留在loop内
2. 